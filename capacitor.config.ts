import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.toweruniverse.game',
  appName: 'Tower build universe',
  webDir: 'dist',

};

export default config;
